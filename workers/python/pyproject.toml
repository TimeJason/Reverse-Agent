[project]
name = "software-analysis-worker"
version = "0.1.0"
description = "Python worker for Software Analysis MCP"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=9.0.2"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
